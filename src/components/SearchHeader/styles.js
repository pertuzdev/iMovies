import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    paddingVertical: 24,
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
  iconContainer: {
    flexGrow: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  searchbar: {
    flexGrow: 9,
    marginRight: 16,
  },
});
